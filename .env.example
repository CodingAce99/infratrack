# Clave de cifrado AES-256-GCM para credenciales SSH en base de datos
# Generar con PowerShell:
# [Convert]::ToBase64String((1..32 | ForEach-Object { [byte](Get-Random -Maximum 256) }))
INFRATRACK_ENCRYPTION_KEY=your_base64_encoded_32_byte_key_here